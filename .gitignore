# MCP config (local)
.mcp.json

# Book binaries do NOT belong here — put them in ~/primary/repos/library/ instead.
# This folder holds project-local quote extractions (.md files) only.
source-extracts/**/*.pdf
source-extracts/**/*.epub
source-extracts/**/*.mobi
source-extracts/**/*.djvu
source-extracts/**/*.zip

# OCR working pages (local)
source-extracts/.ocr_pages/

# Temporary read artifacts
tmp_read/

